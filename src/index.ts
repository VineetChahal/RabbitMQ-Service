/*
import { sendToQueue } from "../rabbitmq/producer";

const testMessage = {
  email: "test@example.com",
  subject: "Welcome!",
  body: "Hello from RabbitMQ!"
};

sendToQueue(testMessage).catch(console.error);
*/